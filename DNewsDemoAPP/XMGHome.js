import React from 'react';
import {
    StyleSheet,
    View,
    Text,
    TabBarIOS,
    NavigatorIOS,
} from 'react-native';

class Home extends React.Component{
    render() {
        return(
            <View>
                <Text>我只框架页</Text>
            </View>
        )
    }
}

let styles = new StyleSheet.create({
    container: {
    }
});

export default Home;